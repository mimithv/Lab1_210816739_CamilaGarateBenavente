#lang racket
;; ==========================================
;; Main del sistema Biblioteca Virtual
;; ==========================================
;; Este archivo importa todos los TDAs y expone
;; sus funciones principales para ser utilizadas
;; en el script de pruebas u otros módulos.
;; ==========================================

(provide (all-defined-out))

(require "tda-usuario.rkt")
(require "tda-libro.rkt")      ;; futuro
(require "tda-prestamo.rkt")   ;; futuro
(require "tda-sistema.rkt")    ;; futuro
